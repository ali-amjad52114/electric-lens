package com.electriclens.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Electric Lens palette — dark industrial field-tool look.
 * These named constants are the single source of truth; screen agents should
 * reference them directly (or via MaterialTheme.colorScheme where appropriate).
 */
val BgDark = Color(0xFF071018)      // background
val PanelDark = Color(0xFF121B24)   // panels / cards
val Caution = Color(0xFFF5A000)     // caution amber (primary action / warnings)
val Verified = Color(0xFF36D27F)    // verified green (done / success)
val Alert = Color(0xFFEF4444)       // alert red (live / danger)
val TextLight = Color(0xFFF4F7FB)   // primary text
